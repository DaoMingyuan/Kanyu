# 安全政策 (Security Policy)

## 支持的版本

| 版本 | 支持状态 |
|------|---------|
| 0.1.x | ✅ 当前开发分支，接受安全修复 |

## 报告漏洞

**请勿在公开 Issue 中报告安全漏洞。**

请发送邮件至 **345558033@qq.com**，包含：

- 受影响的组件与版本
- 复现步骤或概念验证（PoC）
- 潜在影响评估

我们将在 72 小时内确认收到，并在修复发布后公开致谢（除非你希望匿名）。

## 安全设计基线

堪舆在架构层面的安全承诺（贡献者必须遵守）：

1. **无任意代码执行**：MCP 接口只暴露声明式工具，永不提供 `execute_code` 类能力。
2. **不可信解析器沙箱化**：历史上存在 CVE 的 C 解析库（如 LibreDWG）只能编译为
   WASM 在 wasmtime 沙箱中运行，不得直接链接进内核进程。
3. **堪舆灵伦理约束**（总规 §4.4）：AI 层不能修改自己的目标函数；AI 生成的代码
   必须经沙箱验证与人类审核后才能合并内核。
4. **依赖审计**：CI 通过 `cargo-deny` 检查许可证与已知漏洞（RustSec advisory-db）。
